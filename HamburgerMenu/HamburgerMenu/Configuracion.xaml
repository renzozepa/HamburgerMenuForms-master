<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HamburgerMenu.Configuracion">
    <ScrollView >
        <FlexLayout Margin="10" Direction="Column" AlignContent="Center" JustifyContent="SpaceAround">
            <Frame Margin="5" BorderColor="White" BackgroundColor="White" HorizontalOptions="Center">
                <StackLayout Margin="5">
                    <StackLayout Orientation="Horizontal">
                        <Switch x:Name="AlmLocal" />
                        <Label Text="Almacenar local"  HorizontalOptions="StartAndExpand" FontSize="14" />
                    </StackLayout>
                    <StackLayout Orientation="Horizontal">
                        <Switch x:Name="AlmServer" />
                        <Label Text="Almacenar servidor"  HorizontalOptions="StartAndExpand" FontSize="14" />
                    </StackLayout>
                    <StackLayout Orientation="Horizontal">
                        <Switch x:Name="AlmLocalServer" />
                        <Label Text="Almacenar local y servidor"  HorizontalOptions="StartAndExpand" FontSize="14" />
                    </StackLayout>
                </StackLayout>
            </Frame>
        </FlexLayout>
    </ScrollView>
</ContentPage>